apiVersion: v1
kind: DeploymentConfig
metadata:
  labels:
    CodiAplicacio: "0615"
    CodiEntitat: ENS
    CodiInap: XXX
    CodiServei: "7_XXXX"
    Entorn: PRE
    app: simtest-app
  name: simtest-app
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: simtest-app
    spec:
      containers:
        - image: image-registry.openshift-image-registry.svc:5000/suport-tecdigitals-dev/simtest-app:1.0.2
          name: simtest-app
          imagePullPolicy: Always
          ports:
            - containerPort: 1080
              protocol: TCP
          resources:
            limits:
              cpu: 250m
              memory: 256Mi
            requests:
              cpu: 250m
              memory: 256Mi

