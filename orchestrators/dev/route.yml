apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: simtest-app
  name: simtest-app
  namespace: suport-tecdigitals-dev
spec:
  host: simtest-app-suport-tecdigitals-dev.apps.sandbox-m2.ll9k.p1.openshiftapps.com
  port:
    targetPort: http
  to:
    kind: Service
    name: simtest-app
    weight: 100
  wildcardPolicy: None
